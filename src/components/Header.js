function Header() {
    return (
      <div className="header-banner">
        <h1 className="claimit-header">Claimit</h1>
        <p>A self-service web UI for managing and claiming orphaned file(s) ownership or deleting within your usecase/application directories [mapr volumes].</p>
    </div>
    );
}
  
export default Header;